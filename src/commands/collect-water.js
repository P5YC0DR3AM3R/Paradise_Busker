const collectWater = (user, args) => {
  return `<@${user.id}> hauled in 500 barrels o' water! Beware, matey! The water'll spoil in 3 days' time if it rests in them barrels.`;
};

module.exports = collectWater;
